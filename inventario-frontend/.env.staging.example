# API HTTPS exclusiva del entorno de staging.
# Mantener frontend y API dentro del mismo sitio registrable para conservar
# el contrato SameSite=Strict de la cookie de sesión.
VITE_API_URL=https://api-inventario-staging.example.invalid/api
